<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".UI.Home.PrincipalPage"
    android:fitsSystemWindows="true"
    android:id="@+id/body_container">

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation"
        />
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent">
        <!--Bloque Medio-->
        <androidx.appcompat.widget.LinearLayoutCompat
            android:id="@+id/block_medio"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="top"
            android:orientation="vertical">
            <com.lorentzos.flingswipe.SwipeFlingAdapterView
                android:id="@+id/frame"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="top"
                android:visibility="gone"
                app:max_visible="4"
                app:min_adapter_stack="6"
                app:rotation_degrees="15.5" />
        </androidx.appcompat.widget.LinearLayoutCompat>

    </RelativeLayout>
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:itemBackground="@color/white"
        app:itemIconTint="@color/gray"
        app:itemActiveIndicatorStyle="@null"
        app:labelVisibilityMode="unlabeled"
        app:menu="@menu/menu_nav" />

    <include layout="@layout/screen_match" />
    <!--cuando le dan click a mas informacion-->
    <include android:id="@+id/screen_info" layout="@layout/screen_more_info" android:visibility="gone"/>
</RelativeLayout>